{
  "devDependencies": {
    "autoprefixer": "^10.3.3",
    "postcss": "^8.3.6",
    "prettier": "^2.3.2",
    "strip-css-comments-cli": "^3.0.0",
    "tailwindcss": "^2.2.8"
  },
  "scripts": {
    "build": "NODE_ENV=production npx tailwindcss -i ./style.css | npx strip-css-comments --no-preserve | npx prettier --stdin-filepath foo.css | grep '\\S' > ./tailwind.css",
    "lint": "prettier --write *.js *.css *.html *.json *.yml _layouts/*.html"
  }
}
